import { tv } from 'tailwind-variants';

export const variants = tv({
  variants: {
    variant: {
      primary: '',
      secondary: '',
      success: '',
      error: '',
      none: '',
    },
  },
});
